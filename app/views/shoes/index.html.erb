<h1>OBUVnB</h1>

<h2>Flashy shoes for rent.</h2>
<div class="flexbox">
  <% @shoes.each do |shoe| %>
    <%= link_to shoe_path(shoe), class: "text-decoration-none" do %>
      <div class="cards">
  <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path shoe.image1.key %>)">
    <%= shoe.model %> <%= shoe.price %> €
  </div>
</div>
    <% end %>
<% end %>
</div>
